---
layout: category-page.njk
title: Latest Scholarships in Kenya
description: Find the latest scholarship opportunities for Kenyan students. Browse through our curated list of scholarships updated daily.
pagination:
  data: collections.scholarship
  size: 10
  reverse: true
  alias: posts
---

<h1>Latest Scholarship Opportunities</h1>

<div class="opportunities-grid">
{% for post in posts %}
    <article class="opportunity-card">
        <div class="opportunity-meta">
            <span class="category scholarship">Scholarship</span>
            <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date }}</time>
        </div>
        
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        
        <div class="opportunity-details">
            {% if post.data.organization %}
            <span class="organization">{{ post.data.organization }}</span>
            {% endif %}
            {% if post.data.level %}
            <span class="level">{{ post.data.level }}</span>
            {% endif %}
        </div>

        <div class="opportunity-preview">
            {{ post.data.description }}
        </div>

        {% if post.data.isNew %}
            <span class="badge new">NEW</span>
        {% endif %}

        {% if post.data.deadline %}
        <div class="deadline">
            Deadline: {{ post.data.deadline }}
        </div>
        {% endif %}

        <a href="{{ post.url }}" class="read-more">Read More</a>
    </article>
{% endfor %}

{% if posts.length === 0 %}
<div class="no-opportunities">
    <p>No scholarship opportunities available at the moment. Please check back later.</p>
</div>
{% endif %}
</div>

<nav class="pagination">
    {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">Previous</a>
    {% endif %}
    {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">Next</a>
    {% endif %}
</nav> 