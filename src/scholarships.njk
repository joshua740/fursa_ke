---
layout: base.njk
title: Scholarships
---

<h1>Latest Scholarship Opportunities</h1>

<div class="opportunities-grid">
{% for post in collections.scholarships %}
    <article class="opportunity-card">
        <div class="opportunity-meta">
            <span class="category scholarship">Scholarship</span>
            <time datetime="{{ post.date | dateFormat }}">{{ post.date | smartTime }}</time>
        </div>
        
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        
        <div class="opportunity-preview">
            {{ post.data.description }}
        </div>

        {% if post.data.isNew %}
            <span class="badge new">NEW</span>
        {% endif %}

        <a href="{{ post.url }}" class="read-more">Read More</a>
    </article>
{% endfor %}
</div> 