---
layout: category-page.njk
title: Latest Jobs in Kenya
description: Find the latest job opportunities from top companies in Kenya. Browse through our curated list of jobs updated daily.
pagination:
  data: collections.job
  size: 10
  alias: posts
---

<div class="jobs-page">
    <div class="page-header">
        <h1>Latest Jobs</h1>
    </div>

    <div class="opportunities-grid" id="jobsList">
        {% for post in posts %}
            <article class="opportunity-card" data-category="{{ post.data.jobCategory | default('other') }}">
                <div class="opportunity-meta">
                    <span class="category job">Job</span>
                    <time datetime="{{ post.date | isoDate }}">{{ post.date | formatDate }}</time>
                </div>
                
                <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
                
                <div class="opportunity-details">
                    {% if post.data.company %}
                    <span class="company">{{ post.data.company }}</span>
                    {% endif %}
                    {% if post.data.location %}
                    <span class="location">{{ post.data.location }}</span>
                    {% endif %}
                    {% if post.data.type %}
                    <span class="job-type">{{ post.data.type }}</span>
                    {% endif %}
                </div>

                <div class="opportunity-preview">
                    {{ post.data.description }}
                </div>

                {% if post.data.isNew %}
                    <span class="badge new">NEW</span>
                {% endif %}

                {% if post.data.deadline %}
                <div class="deadline">
                    Deadline: {{ post.data.deadline }}
                </div>
                {% endif %}

                <a href="{{ post.url }}" class="read-more">Read More</a>
            </article>
        {% endfor %}

        {% if posts.length === 0 %}
        <div class="no-opportunities">
            <p>No job opportunities available at the moment. Please check back later.</p>
        </div>
        {% endif %}
    </div>

    <nav class="pagination">
        {% if pagination.href.previous %}
            <a href="{{ pagination.href.previous }}">Previous</a>
        {% endif %}
        {% if pagination.href.next %}
            <a href="{{ pagination.href.next }}">Next</a>
        {% endif %}
    </nav>

    <div class="jobs-footer">
        <div class="post-job">
            <h2>Have a Job to Post?</h2>
            <p>If you're an employer looking to reach qualified candidates, contact us to feature your job listing.</p>
            <a href="/contact/" class="button">Contact Us</a>
        </div>
    </div>
</div> 