---
layout: base.njk
title: Fursa Kenya
description: Find the latest jobs, internships, and scholarships in Kenya. Fursa KE connects you with opportunities from top companies and organizations.
---

<section class="homepage-section latest-opportunities">
  <h1>Latest Opportunities</h1>

  <div class="opportunities-grid">
  {% set posts = collections.allPosts | reverse %}
  {% for post in posts.slice(0,6) %}
      <article class="post-box compact horizontal">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <div class="post-meta">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          </div>
      </article>
  {% endfor %}
  </div>
</section>

<section class="homepage-section category-sections">
  <div class="section jobs">
    <div class="section-header">
      <h2>Latest Jobs</h2>
    </div>
    <div class="opportunities-grid">
    {% set jobPosts = collections.job %}
    {% for post in jobPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact horizontal">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <div class="post-meta">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
    <div class="section-footer">
      <a href="/jobs/" class="view-all">View All Jobs</a>
    </div>
  </div>

  <div class="section internships">
    <div class="section-header">
      <h2>Latest Internships</h2>
    </div>
    <div class="opportunities-grid">
    {% set internshipPosts = collections.internship %}
    {% for post in internshipPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact horizontal">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <div class="post-meta">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
    <div class="section-footer">
      <a href="/internships/" class="view-all">View All Internships</a>
    </div>
  </div>

  <div class="section scholarships">
    <div class="section-header">
      <h2>Latest Scholarships</h2>
    </div>
    <div class="opportunities-grid">
    {% set scholarshipPosts = collections.scholarship %}
    {% for post in scholarshipPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact horizontal">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <div class="post-meta">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
    <div class="section-footer">
      <a href="/scholarships/" class="view-all">View All Scholarships</a>
    </div>
  </div>
</section> 