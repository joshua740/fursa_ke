---
layout: base.njk
title: Latest Opportunities
description: Find the latest jobs, internships, and scholarships in Kenya. Fursa KE connects you with opportunities from top companies and organizations.
pagination:
  data: collections.allPosts
  size: 8
  reverse: true
  alias: posts
---

<section class="homepage-section latest-opportunities">
  <h1>Latest Opportunities</h1>

  <div class="opportunities-grid">
  {% for post in posts %}
      <article class="post-box compact">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
              <div class="post-details">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">Posted on {{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
          </div>
      </article>
  {% endfor %}
  </div>
</section>

<section class="homepage-section category-sections">
  <div class="section jobs">
    <div class="section-header">
      <h2>Latest Jobs</h2>
      <a href="/jobs/" class="view-more">View More Jobs</a>
    </div>
    <div class="opportunities-grid">
    {% set jobPosts = collections.job %}
    {% for post in jobPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
              <div class="post-details">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">Posted on {{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
  </div>

  <div class="section internships">
    <div class="section-header">
      <h2>Latest Internships</h2>
      <a href="/internships/" class="view-more">View More Internships</a>
    </div>
    <div class="opportunities-grid">
    {% set internshipPosts = collections.internship %}
    {% for post in internshipPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
              <div class="post-details">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">Posted on {{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
  </div>

  <div class="section scholarships">
    <div class="section-header">
      <h2>Latest Scholarships</h2>
      <a href="/scholarships/" class="view-more">View More Scholarships</a>
    </div>
    <div class="opportunities-grid">
    {% set scholarshipPosts = collections.scholarship %}
    {% for post in scholarshipPosts %}
    {% if loop.index <= 4 %}
      <article class="post-box compact">
          <div class="post-thumbnail">
              <a href="{{ post.url }}">
              {% if post.data.thumbnail %}
                  <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" loading="lazy">
              {% else %}
                  <img src="/images/default-thumbnail.jpg" alt="Default thumbnail" loading="lazy">
              {% endif %}
              </a>
          </div>
          <div class="post-content">
              <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
              <div class="post-details">
                  <time datetime="{{ post.date | date('YYYY-MM-DD') }}">Posted on {{ post.date | date('MMMM D, YYYY') }}</time>
                  {% if post.data.location %}
                  <span class="location">{{ post.data.location }}</span>
                  {% endif %}
              </div>
          </div>
      </article>
    {% endif %}
    {% endfor %}
    </div>
  </div>
</section>

{% if pagination.href.previous or pagination.href.next %}
<nav class="pagination">
    {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">Previous</a>
    {% endif %}
    {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">Next</a>
    {% endif %}
</nav>
{% endif %} 